#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(int argc, char const *argv[])
{
	ll n, k;
	cin>>n>>k;
	vector<ll> mv;
	for( ll i = 0; i < n; i++ )
	{
		ll z;
		mv.push_back(z);
	}	
	
	return 0;
}