#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[]) {
  long long a, b;
  cin >> a >> b;
  if(a*b%2 == 0)
    cout << "Malvika\n";
  else
    cout << "Akshat\n";
  return 0;
}
